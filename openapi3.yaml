openapi: 3.0.1
info:
  title: json-id-merger
  description: merge external osm mapping with internal osm mapping
  version: 1.0.0
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT
servers:
  - url: http://localhost:8080
paths:
  /merge:
    post:
      operationId: merger
      tags:
        - Merger
      summary: merger two id json to one
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/JsonId'
      responses:
        200:
          description: Ok
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JsonIdMerged'
        400:
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/error'
        422:
          description: Unprocessable Entity
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/error'
        500:
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/error'
components:
  schemas:
    error:
      type: object
      required:
        - message
      properties:
        message:
          type: string
    JsonIdMerged:
      type: array
      items:
        type: object
        properties:
          externalId:
            type: string
          osmId:
            type: number
    JsonId:
      type: object
      required:
        - externalMapping
        - osmMapping
      properties:
        externalMapping:
          type: array
          items:
            type: object
            properties:
              tempOsmId:
                type: number
              externalId:
                type: string
            additionalProperties: false
        osmMapping:
          type: array
          items:
            type: object
            properties:
              tempOsmId:
                type: number
              osmId:
                type: number
            additionalProperties: false
